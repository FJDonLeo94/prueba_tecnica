# import gradio as gr
from utils import products

inputs = 0
'''
input_components = [
    gr.inputs(step=1, label="Vaucher"),
    gr.inputs(step=1, label="T-shirt"),
    gr.inputs(step=1, label="Pants")
]

output_component = gr.outputs.Label(label="El precio asciende a")
'''